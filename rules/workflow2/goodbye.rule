# We need to do preprocessing
# to construct the right output file

goodbye_file = "goodbye.txt"

rule goodbye_target:
    """
    Shell commands to say goodbye to <name>
    """
     output:
        "goodbye.txt"
     shell:
        "echo goodbye {name} > {output}"
