# Need to preprocess, still,
# to get extension
# to construct the right
# input file

hello_file   = "hello.txt"
goodbye_file = "goodbye.txt"
master_file  = "master.txt"

rule master:
    """
    Shell commands to say goodbye to <name>
    """
    input:
        hello_file
        goodbye_file
    output:
        master_file

